# Email Forwarder Worker 配置
# 部署到每个 CF 账户，用于将邮件转发到统一的 mail-api 后端

name = "email-forwarder"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# 环境变量 - 部署时需要设置
[vars]
API_URL = "https://mail-api.netpulsex.icu"

# API_KEY 使用 secret 设置:
# wrangler secret put API_KEY
